recObj = audiorecorder(48000,16,1)
recDuration = 1;
recordblocking(recObj, recDuration);
letter = ""

nullCounter = 0; MAX_COUNT = 10;
while (nullCounter < MAX_COUNT)
    recordblocking(recObj, recDuration);
    
end
pitches
    

